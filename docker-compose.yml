services:
  ai-resume-screener:
    build: .
    container_name: ai-resume-screener
    ports:
      - "8002:8002"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
    restart: unless-stopped